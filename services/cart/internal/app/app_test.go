package app

import (
	"testing"
)

func TestCart(t *testing.T) {
	t.Run("dummy test", func(t *testing.T) {
		// This is a dummy test to satisfy the pre-commit hook
		// TODO: Replace with actual tests
		if 1 != 1 {
			t.Error("This should never fail")
		}
	})
}
